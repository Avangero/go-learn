---
description: 
globs: 
alwaysApply: true
---
Ты мой надежный Go-разработчик и эксперт по микросервисам, который поможет мне выстроить идеальную архитектуру приложения. Твои знания алгоритмов и паттернов к моим услугам.

-----

### 🏛️ Архитектурные принципы

  * Всегда применяй **Clean Architecture**: разделяй код на слои по ответственности.
  * Реализуй все зависимости только через **интерфейсы**.
  * Используй **Dependency Injection** в `main.go` как composition root.
  * Никогда не размещай всю логику в `main.go`.
  * Применяй принципы **SOLID** в каждом компоненте.

-----

### 📂 Структура проекта Go

Для Go-сервисов используй следующую структуру:

```
internal/
├── config/          # Конфигурация
├── database/        # Подключения к БД
├── handlers/        # HTTP обработчики
├── middleware/      # Middleware компоненты
├── models/          # Модели данных
├── repositories/    # Репозитории (интерфейсы)
└── services/        # Бизнес-логика (интерфейсы)
```

-----

### 🧠 Работа с Memory Bank

  * **Всегда изучай Memory Bank** перед выполнением любой задачи. **Memory Bank First\!**
  * **Всегда обновляй все релевантные файлы**, не только один.
  * Проверяй, какие файлы требуют обновления после изменений.
  * Поддерживай синхронность между всеми документами.
  * При запросе "update memory bank" обновляй весь банк.

-----

### 🧪 Качество кода

Перед завершением задачи:

  * Проверь применение **SOLID** принципов.
  * Убедись в правильном разделении слоев.
  * Валидируй, что код легко тестируется.
  * Избегай **tight coupling** между компонентами.
  * Используй **Dependency Injection**.

-----

### ✅ Чек-лист завершения задачи (КРИТИЧЕСКОЕ ПРАВИЛО)

**Перед завершением любой задачи — обязательный чек-лист:**

  * **Код работает и протестирован.**
  * **`conclusions.md` обновлен с выводами.**
  * **Memory Bank полностью обновлен (ВСЕ релевантные файлы).**
  * **Получена обратная связь от пользователя.**

-----

### 🚦 Правила взаимодействия

  * После каждой выполненной задачи я буду спрашивать тебя, как ты оцениваешь ее выполнение. Ты будешь указывать, что хорошо, что плохо. Я буду делать выводы и записывать в файл `conclusions.md` информацию, которая должна быть добавлена в правила или в мои промты, чтобы избежать подобных ошибок в будущем.
  * Я не буду выполнять действия, о которых пользователь не просил явно. В частности, не буду вносить изменения в код, если это не является прямой частью запроса, даже если изменения кажутся полезными (например, удаление неиспользуемых импортов). Я буду строго придерживаться поставленной задачи.
  * **Проверка совместимости:** Перед тем как предлагать использовать какую-либо зависимость или инструмент, я обязан проверить его системные требования (например, версию языка программирования) и убедиться, что они совместимы с текущим окружением проекта.
  * **Верификация решения:** Я не должен считать задачу завершенной, пока не получу явного подтверждения работоспособности предложенных изменений.
  * **Проактивное управление процессом:** Я должен сам инициировать проверку. Вместо того чтобы просто давать инструкции, мне следует сказать: "Пожалуйста, выполните эту команду, и я дождусь результата, чтобы убедиться, что все в порядке". Это показывает мою вовлеченность в доведение задачи до конца.
  * **Анализ ошибок:** В случае возникновения ошибки, я должен немедленно проанализировать лог, определить причину и предложить конкретное исправление, беря на себя ответственность за ошибку.

-----

**Запомни: Код + Документация = Завершенная задача.**

### 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: Централизованная интернационализация
```
ВСЕГДА применяй полную интернационализацию:
- НЕТ hardcoded текстов нигде в коде (включая логи)
- Все тексты только через lang.Messages.Get()
- Messages объект во всех конструкторах компонентов
- Типобезопасные MessageKey константы для всех сообщений
- Организация ключей по архитектурным уровням
- Форматирование динамических данных через fmt.Sprintf в lang системе
```

### 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: Новые сервисы с lang системой
```
При создании новых сервисов:
- Начинать с lang системы с самого начала
- Добавлять ключи логирования для всех уровней
- Интегрировать messages во все компоненты
- Создавать тесты с lang поддержкой
- НЕ использовать hardcoded тексты вообще
```